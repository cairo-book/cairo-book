# Layout

The memory layout in Cairo's memory model is how the segments are organized and initialized for a given execution. Cairo programs can run with different layout configurations depending on the program's builtin requirements however, the general structure remains the same. At a high level, it is arranged like...
 