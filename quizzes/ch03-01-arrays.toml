[[questions]]
id = "0xaaaaaa"
type = "MultipleChoice"
prompt.prompt = """
How do we remove 12 from this array?
```rust
fn main() {
    let mut arr = ArrayTrait::new();
    arr.append(12);
    arr.append(6);
    arr.append(3);
}
```
"""
answer.answer = "`arr.pop_front()`"
prompt.distractors = [
  "`arr.pop()`"  
]




[[questions]]
id = "0xbbbbbbb"
type = "MultipleChoice"
prompt.prompt = """
What is the best way to store "John", 100 and 1000?
"""
answer.answer = "`enum`"
prompt.distractors = [
"`array.span()`",
"`array!`"
]
context = """
Enum is used to store multiple data types.
"""





[[questions]]
id = "0xcccccc"
type = "MultipleChoice"
prompt.prompt = """
What happens when you run this function?
```rust
fn main() -> u8 {
    let mut arr = ArrayTrait::<u8>::new();
    arr.append(100);
    arr.append(2);

    let x = *arr.at(1);

    println!("The first value is {}", x); 
}
```
"""
answer.answer = "It panicks"
prompt.distractors = [
  "Returns 2",
  "Returns 100"
]

context = """
The function expects `u8` return variable but it does not return anything.
"""


