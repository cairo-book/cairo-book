[package]
name = "runner_crate"
version = "0.1.0"
edition = "2024_07"

# See more keys and their definitions at https://docs.swmansion.com/scarb/docs/reference/manifest

[[target.executable]]

[cairo]
enable-gas = false

# Don't make this executable - we use it for quiz validation.
[dependencies]
cairo_execute = "2.20.0"

[dev-dependencies]
snforge_std = "0.62.1"
assert_macros = "2.20.0"

[scripts]
test = "snforge test"

[tool.scarb]
allow-prebuilt-plugins = ["snforge_std"]
