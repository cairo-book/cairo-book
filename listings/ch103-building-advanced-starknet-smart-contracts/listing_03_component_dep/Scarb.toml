[package]
name = "listing_03_component_dep"
version = "0.1.0"
edition = "2024_07"

# See more keys and their definitions at https://docs.swmansion.com/scarb/docs/reference/manifest

[dependencies]
starknet = ">=2.12.0"

[[target.starknet-contract]]

[dev-dependencies]
snforge_std = "0.48.0"
assert_macros = "2.12.0"

[scripts]
test = "snforge test"

[tool.scarb]
allow-prebuilt-plugins = ["snforge_std"]

[cairo]
enable-gas = true
