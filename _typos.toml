[default]
extend-ignore-identifiers-re = ["Groth16", "result_ba"]

[type.po]
extend-glob = ["*.po", "*.css", "*.js"]
check-file = false

[files]
extend-exclude = ["po/*.po", "listings/**/*.json", "**/*.excalidraw"]
